package Domain;

public class Worker extends Person{

    /**
     * Конструктор класса Worker:
     * @param name - имя.
     * @param age - возраст.
     */
    public Worker(String name, int age) {
        super(name, age);
    }
}
